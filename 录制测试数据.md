# 轻课台轻量版录制测试数据

这份数据用于复现教师端和学生端的视频流程。当前版本是轻量演示登录，登录不校验密码，账号只用于区分角色。

## 登录账号

| 角色 | 姓名 | 账号 | 密码 |
| --- | --- | --- | --- |
| 教师 | 王老师 | `teacher@example.com` | `123456` |
| 学生 | 张三 | `student@example.com` | `123456` |

## 班级与课程

```json
{
  "classId": "class_python",
  "className": "Python 基础班",
  "courseName": "Python 程序设计",
  "term": "2026 春季学期",
  "joinCode": "PY2026",
  "teacherName": "王老师"
}
```

## 三组测试数据

### A 组：全员提交

用于验证教师发布作业、学生完成作业、教师查看提交的主流程。

```json
{
  "name": "循环结构全员练习",
  "chapterId": "chapter_3",
  "description": "使用 for 循环输出 1 到 10，并说明循环变量的变化。",
  "deadline": "2026-09-15",
  "students": ["student_1", "student_2", "student_3"],
  "submissions": [
    {"studentId": "student_1", "content": "使用 for i in range(1, 11) 输出 1 到 10。"},
    {"studentId": "student_2", "content": "使用 while 循环完成同样的输出。"},
    {"studentId": "student_3", "content": "比较 for 和 while 的适用场景。"}
  ]
}
```

### B 组：部分提交

用于验证教师查看未提交学生，以及学生端只看到自己的作业状态。

```json
{
  "name": "条件判断部分提交",
  "chapterId": "chapter_2",
  "description": "判断输入成绩对应的等级：优秀、良好、及格或不及格。",
  "deadline": "2026-09-18",
  "students": ["student_1", "student_2", "student_3"],
  "submissions": [
    {"studentId": "student_1", "content": "使用 if / elif / else 完成成绩分级。"},
    {"studentId": "student_2", "content": "补充边界值 60、70、90 的判断。"}
  ],
  "notSubmitted": ["student_3"]
}
```

### C 组：重复提交覆盖

用于验证同一个学生重复提交时，后一次内容覆盖前一次内容，不产生重复记录。

```json
{
  "name": "变量复习重复提交",
  "chapterId": "chapter_1",
  "description": "解释变量、字符串、整数和布尔值的区别。",
  "deadline": "2026-09-20",
  "studentId": "student_2",
  "firstContent": "变量可以保存一个值。",
  "finalContent": "变量是指向对象的名称；字符串、整数和布尔值属于不同数据类型。",
  "expectedUniqueSubmissionCount": 1
}
```

## 复现顺序

1. 打开站点，选择“教师登录”，点击“进入教师端”。
2. 在“作业管理”中新建 A、B、C 三组作业；章节分别选择第 3、2、1 章。
3. 切换到“学生登录”，点击“进入学生端”，在“我的作业”提交 A 组作业。
4. 再回到教师端进入“学生提交”，确认出现待处理提交。
5. 用 B 组只提交前两名学生，确认第三名保持未提交。
6. 用 C 组对同一学生提交两次，确认最终内容为 `finalContent` 且记录数为 1。

> 当前页面的“提交作业”按钮会使用第一位演示学生和名称中包含“循环”的作业，因此录制主流程使用 A 组最稳定；B、C 组可通过 `scripts/test-backend.mjs` 完整验证接口行为。
